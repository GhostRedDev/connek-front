import 'package:flutter/material.dart';

class EmptySpaceTopWidget extends StatelessWidget {
  const EmptySpaceTopWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox(height: 20);
}

class EmptySpaceWidget extends StatelessWidget {
  const EmptySpaceWidget({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox(height: 100);
}
