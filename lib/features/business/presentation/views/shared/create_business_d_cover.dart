import 'package:flutter/material.dart';

class CreateBusinessDCover extends StatelessWidget {
  const CreateBusinessDCover({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Business Cover')),
      body: const Center(child: Text('Select Business Cover')),
    );
  }
}
