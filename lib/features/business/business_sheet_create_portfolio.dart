import 'package:flutter/material.dart';

class BusinessSheetCreatePortfolio extends StatelessWidget {
  const BusinessSheetCreatePortfolio({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Portfolio')),
      body: const Center(child: Text('Create Portfolio Form')),
    );
  }
}
