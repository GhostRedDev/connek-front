import 'package:flutter/material.dart';

class CreateBusinessStep4 extends StatelessWidget {
  const CreateBusinessStep4({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Business - Step 4')),
      body: const Center(child: Text('Step 4 Content')),
    );
  }
}
