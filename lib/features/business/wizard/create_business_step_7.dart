import 'package:flutter/material.dart';

class CreateBusinessStep7 extends StatelessWidget {
  const CreateBusinessStep7({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Business - Step 7')),
      body: const Center(child: Text('Step 7 Content')),
    );
  }
}
