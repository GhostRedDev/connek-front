import 'package:flutter/material.dart';

class CreateBusinessStep8 extends StatelessWidget {
  const CreateBusinessStep8({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Business - Step 8')),
      body: const Center(child: Text('Step 8 Content')),
    );
  }
}
