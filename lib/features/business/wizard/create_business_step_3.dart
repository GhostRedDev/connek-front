import 'package:flutter/material.dart';

class CreateBusinessStep3 extends StatelessWidget {
  const CreateBusinessStep3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Business - Step 3')),
      body: const Center(child: Text('Step 3 Content')),
    );
  }
}
