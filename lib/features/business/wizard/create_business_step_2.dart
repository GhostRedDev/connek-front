import 'package:flutter/material.dart';

class CreateBusinessStep2 extends StatelessWidget {
  const CreateBusinessStep2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Business - Step 2')),
      body: const Center(child: Text('Step 2 Content')),
    );
  }
}
