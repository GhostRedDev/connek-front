import 'package:flutter/material.dart';

class CreateBusinessStep1 extends StatelessWidget {
  const CreateBusinessStep1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Business - Step 1')),
      body: const Center(child: Text('Step 1 Content')),
    );
  }
}
