import 'package:flutter/material.dart';

class CreateBusinessStep5 extends StatelessWidget {
  const CreateBusinessStep5({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Business - Step 5')),
      body: const Center(child: Text('Step 5 Content')),
    );
  }
}
