import 'package:flutter/material.dart';

class CreateBusinessStep6 extends StatelessWidget {
  const CreateBusinessStep6({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Business - Step 6')),
      body: const Center(child: Text('Step 6 Content')),
    );
  }
}
