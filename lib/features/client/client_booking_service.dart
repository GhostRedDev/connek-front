import 'package:flutter/material.dart';

class ClientBookingService extends StatelessWidget {
  const ClientBookingService({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Booking Service')),
      body: const Center(child: Text('Booking Flow')),
    );
  }
}
